<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authorize</title>
</head>
<body>
    <h2>Choose a login provider:</h2>
    <ul>
        <li><a href="{{ url_for('google') }}"><button>Login with Google</button></a></li>
        <li><a href="{{ url_for('microsoft.login') }}"><button>Login with Microsoft</button></a></li>
        <li><a href="{{ url_for('gitlab.login') }}"><button>Login with GitLab</button></a></li>
        <li><a href="{{ url_for('github.login') }}"><button>Login with GitHub</button></a></li>
    </ul>
</body>
</html>
